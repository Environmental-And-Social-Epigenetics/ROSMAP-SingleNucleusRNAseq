# QC (Archive)

Legacy per-sample QC scripts superseded by the Pipeline.

- `Doublets/` — Legacy doublet detection scripts (now handled by Pipeline Stage 2)
- `Outliers/` — Legacy outlier removal scripts (now handled by Pipeline Stage 1)

Kept for reference only. Use `Processing/Tsai/Pipeline/` for current processing.
