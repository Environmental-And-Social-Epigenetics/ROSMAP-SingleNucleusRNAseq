name: bcftools_env
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bcftools>=1.17
  - samtools>=1.17
  - htslib>=1.17
# Used for BAM/VCF filtering in the DeJager Demuxlet preprocessing step.
