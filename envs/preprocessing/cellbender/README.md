# CellBender Environment

Canonical spec for ambient RNA correction during preprocessing.

```bash
conda env create -f envs/preprocessing/cellbender/environment.yml -p "$CONDA_ENV_BASE/Cellbender_env"
```

