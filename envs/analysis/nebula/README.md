# ACE DEG / Pseudobulk Environment

Canonical spec for ACE DEG preparation and R/Bioconductor pseudobulk workflows.

```bash
conda env create -f envs/analysis/nebula/environment.yml -p "$CONDA_ENV_BASE/nebulaAnalysis7"
```

