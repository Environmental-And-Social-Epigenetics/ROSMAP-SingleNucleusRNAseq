# Stage 3 Integration Environment

Canonical spec for HVG selection, PCA, Harmony, Leiden/UMAP, and ORA annotation.

```bash
conda env create -f envs/processing/stage3_integration/environment.yml -p "$CONDA_ENV_BASE/BatchCorrection_SingleCell"
```

